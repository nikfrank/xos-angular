'use strict';

angular.module('gft')
  .service('schedules', function($http, $q, workouts, exercises, usrs){

      var that = this;

      this.whatever = 'something';

      this.makeSchedule = function(options){
	  // pull each workout
      };


  });
